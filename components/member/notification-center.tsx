"use client";

import Link from "next/link";
import { FormEvent, useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import { createClient } from "@/lib/supabase/client";

export type MemberNotification={id:string;kind:string;title:string;body:string;href:string|null;read_at:string|null;created_at:string};
export type NotificationPreference={in_app_enabled:boolean;email_network:boolean;email_events:boolean;email_support:boolean};
const date=(value:string)=>new Intl.DateTimeFormat("en-KE",{day:"numeric",month:"short",hour:"numeric",minute:"2-digit"}).format(new Date(value));
export function NotificationCenter({userId,notifications,initialPreferences}:{userId:string;notifications:MemberNotification[];initialPreferences:NotificationPreference}){const supabase=useMemo(()=>createClient(),[]);const router=useRouter();const [preferences,setPreferences]=useState(initialPreferences);const [busy,setBusy]=useState(false);const [notice,setNotice]=useState("");useEffect(()=>{const channel=supabase.channel(`notifications:${userId}`).on("postgres_changes",{event:"INSERT",schema:"public",table:"notifications",filter:`user_id=eq.${userId}`},()=>router.refresh()).subscribe();return()=>{void supabase.removeChannel(channel)}},[router,supabase,userId]);async function read(id:string){await supabase.rpc("mark_notification_read",{p_notification_id:id});router.refresh()}async function readAll(){setBusy(true);const {error}=await supabase.rpc("mark_all_notifications_read");setBusy(false);setNotice(error?error.message:"All notifications marked as read.");if(!error)router.refresh()}async function save(event:FormEvent){event.preventDefault();setBusy(true);const {error}=await supabase.rpc("update_notification_preferences",{p_email_events:preferences.email_events,p_email_network:preferences.email_network,p_email_support:preferences.email_support,p_in_app:preferences.in_app_enabled});setBusy(false);setNotice(error?error.message:"Notification preferences saved.")}return <div className="notification-layout"><section className="notification-feed"><header><div><p className="eyebrow">Your updates</p><h1>Notifications</h1></div>{notifications.some(item=>!item.read_at)?<button disabled={busy} onClick={()=>void readAll()}>Mark all read</button>:null}</header>{notifications.length?<div>{notifications.map(item=><article className={item.read_at?"":"unread"} key={item.id}><span className="notification-kind">{item.kind}</span><div><h2>{item.title}</h2><p>{item.body}</p><small>{date(item.created_at)}</small></div>{item.href?<Link href={item.href} onClick={()=>void read(item.id)}>Open</Link>:!item.read_at?<button onClick={()=>void read(item.id)}>Mark read</button>:null}</article>)}</div>:<div className="admin-empty"><strong>You are all caught up</strong><p>Connections, registrations, events and support updates will appear here.</p></div>}</section><aside className="notification-preferences"><p className="eyebrow">Delivery choices</p><h2>Stay meaningfully informed.</h2><p>Essential account, payment and privacy messages remain enabled. Choose how we contact you about everything else.</p><form onSubmit={save}><label><input type="checkbox" checked={preferences.in_app_enabled} onChange={event=>setPreferences(value=>({...value,in_app_enabled:event.target.checked}))}/><span><strong>In-app notifications</strong><small>Show updates in your member account.</small></span></label><label><input type="checkbox" checked={preferences.email_network} onChange={event=>setPreferences(value=>({...value,email_network:event.target.checked}))}/><span><strong>Network email</strong><small>Connection requests and accepted introductions.</small></span></label><label><input type="checkbox" checked={preferences.email_events} onChange={event=>setPreferences(value=>({...value,email_events:event.target.checked}))}/><span><strong>Event email</strong><small>Published event announcements.</small></span></label><label><input type="checkbox" checked={preferences.email_support} onChange={event=>setPreferences(value=>({...value,email_support:event.target.checked}))}/><span><strong>Support email</strong><small>Private support reply alerts.</small></span></label><button className="button button-primary" disabled={busy}>Save preferences</button></form></aside>{notice?<p className="network-message" role="status">{notice}</p>:null}</div>}
