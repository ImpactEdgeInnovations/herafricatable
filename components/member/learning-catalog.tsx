"use client";import Link from"next/link";import{useMemo,useState}from"react";import{createClient}from"@/lib/supabase/client";import{useActionDialog}from"@/components/ui/action-dialog";
export type CourseSummary={course_id:string;slug:string;title:string;summary:string;description:string;instructor_name:string;access_type:"free"|"purchase"|"event_bundle"|"manual";price_minor:number;currency:string;payment_mode:"automatic"|"manual_review"|"closed";status:string;lesson_count:number;enrollment_status:string|null;progress_percent:number};
const money=(amount:number,currency:string)=>new Intl.NumberFormat("en-KE",{style:"currency",currency,maximumFractionDigits:0}).format(amount/100);
export function LearningCatalog({courses}:{courses:CourseSummary[]}){const supabase=useMemo(()=>createClient(),[]);const[busy,setBusy]=useState("");const[message,setMessage]=useState("");const{ask,dialog}=useActionDialog();async function access(course:CourseSummary){setBusy(course.course_id);if(course.access_type==="purchase"&&course.payment_mode==="automatic"){const response=await fetch("/api/payments/paystack/initialize",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({courseId:course.course_id})});const payload=await response.json()as{authorizationUrl?:string;error?:string};if(response.ok&&payload.authorizationUrl){window.location.assign(payload.authorizationUrl);return}setBusy("");setMessage(payload.error??"Checkout could not be initialized.");return}if(course.access_type==="purchase"&&course.payment_mode==="manual_review"){const result=await ask({title:"Submit payment for review",description:"Add the payment information you received or used. An administrator will verify it before granting course access.",confirmLabel:"Submit for review",fields:[{name:"reference",label:"Payment reference (optional)",type:"text",maxLength:160},{name:"note",label:"Payment note",type:"textarea",required:true,minLength:3,maxLength:1000}]});if(!result){setBusy("");return}const reference=String(result.reference);const note=String(result.note);const{error}=await supabase.rpc("create_course_order",{p_course_id:course.course_id,p_manual_note:note,p_manual_reference:reference});setBusy("");setMessage(error?error.message:"Your course purchase is awaiting admin review.");if(!error)window.location.reload();return}const{error}=await supabase.rpc("enroll_in_course",{p_course_id:course.course_id});setBusy("");setMessage(error?error.message:"Course access is ready.");if(!error)window.location.reload()}return <>{dialog}<section className="learning-grid">{courses.map(course=><article key={course.course_id}><div className="learning-card-meta"><span>{course.access_type.replace("_"," ")}</span><small>{course.lesson_count} lesson{Number(course.lesson_count)===1?"":"s"}</small></div><h2>{course.title}</h2><p>{course.summary}</p><div className="learning-instructor">With {course.instructor_name}</div><footer>{course.enrollment_status?<><div><span>{course.progress_percent}% complete</span><div><i style={{width:`${course.progress_percent}%`}}/></div></div><Link className="button button-primary"href={`/learning/${course.slug}`}>Continue</Link></>:course.access_type==="manual"?<span>Access by invitation</span>:course.access_type==="purchase"&&course.payment_mode==="closed"?<span>Enrollment opening soon</span>:<button className="button button-outline"disabled={busy===course.course_id}onClick={()=>void access(course)}>{course.access_type==="purchase"?`Enroll · ${money(course.price_minor,course.currency)}`:course.access_type==="event_bundle"?"Verify event access":"Enroll free"}</button>}</footer></article>)}</section>{message?<p className="network-message"role="status">{message}</p>:null}</>}
