"use client";
import { useMemo, useState } from "react";
import Link from "next/link";
import { createClient } from "@/lib/supabase/client";

export type MemberOrder={created_at:string;currency:string;event:{slug:string;title:string}|null;id:string;processing_mode:string;reference:string;status:string;ticket_name:string;total_minor:number};
export function OrderHistory({orders,refundOrderIds}:{orders:MemberOrder[];refundOrderIds:string[]}){const supabase=useMemo(()=>createClient(),[]);const [busy,setBusy]=useState<string|null>(null);const [message,setMessage]=useState("");async function cancel(order:MemberOrder){const reason=window.prompt("Why are you cancelling this pending registration? (optional)")??"";setBusy(order.id);const {error}=await supabase.rpc("cancel_pending_registration",{p_order_id:order.id,p_reason:reason});setBusy(null);setMessage(error?error.message:"Registration cancelled.");if(!error)window.location.reload()}async function refund(order:MemberOrder){const reason=window.prompt("Tell us why you are requesting a refund (at least 10 characters).")??"";if(reason.trim().length<10)return;setBusy(order.id);const {error}=await supabase.rpc("request_order_refund",{p_order_id:order.id,p_reason:reason});setBusy(null);setMessage(error?error.message:"Refund request submitted for review.");if(!error)window.location.reload()}return <section className="member-orders"><div><p className="eyebrow">Your registrations</p><h2>Orders and access</h2></div>{orders.length?<div>{orders.map(order=><article key={order.id}><div><span>{order.event?.title??"Her Africa Table event"}</span><strong>{order.ticket_name}</strong><small>{order.reference} · {new Intl.DateTimeFormat("en-KE",{day:"numeric",month:"short",year:"numeric"}).format(new Date(order.created_at))}</small></div><div><strong>{order.currency} {(order.total_minor/100).toLocaleString("en-KE",{minimumFractionDigits:2})}</strong><span className="member-status">{order.status.replace("_"," ")}</span></div><div><Link href={`/orders/${order.reference}`}>View receipt</Link>{["pending_payment","pending_review"].includes(order.status)?<button disabled={busy===order.id} onClick={()=>void cancel(order)}>Cancel</button>:null}{order.status==="fulfilled"&&!refundOrderIds.includes(order.id)?<button disabled={busy===order.id} onClick={()=>void refund(order)}>Request refund</button>:null}{refundOrderIds.includes(order.id)?<span>Refund under review</span>:null}</div></article>)}</div>:<div className="admin-empty"><strong>No event orders yet</strong><p>Browse upcoming tables and request your seat.</p><Link className="button button-primary" href="/events">View events</Link></div>}{message?<p className="manager-message" role="status">{message}</p>:null}</section>}
