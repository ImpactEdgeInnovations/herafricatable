"use client";import{FormEvent,useMemo,useState}from"react";import{createClient}from"@/lib/supabase/client";
export type ReferralCampaign={id:string;name:string;description:string;max_referrals_per_member:number;ends_at:string|null};export type MemberReferral={referral_id:string;campaign_id:string;campaign_name:string;code:string;invitee_email:string;relationship:string;vouch:string;status:string;review_note:string|null;created_at:string;claimed_at:string|null;activated_at:string|null};
export function ReferralCenter({campaigns,referrals}:{campaigns:ReferralCampaign[];referrals:MemberReferral[]}){const supabase=useMemo(()=>createClient(),[]);const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);setBusy(true);const{error}=await supabase.rpc("create_vouched_referral",{p_campaign_id:form.get("campaign_id"),p_email:form.get("email"),p_relationship:form.get("relationship"),p_vouch:form.get("vouch")});setBusy(false);setMessage(error?error.message:"Your vouch has been submitted for private review. No access is granted until approval.");if(!error)window.location.reload()}return <><section className="referral-intro"><div><p className="eyebrow">Vouch with intention</p><h2>Invite a woman whose presence strengthens the table.</h2><p>A referral is a statement of trust, not an automatic membership approval. Our team reviews each vouch before sending an invitation.</p></div><form onSubmit={event=>void submit(event)}><label>Campaign<select name="campaign_id"required>{campaigns.map(item=><option value={item.id}key={item.id}>{item.name}</option>)}</select></label><label>Her email address<input name="email"type="email"required/></label><label>How do you know her?<input name="relationship"minLength={3}maxLength={120}required placeholder="Former colleague, founder peer, mentor…"/></label><label>Why are you vouching for her?<textarea name="vouch"minLength={20}maxLength={1200}required placeholder="Share the professional context, values or contribution that makes her right for this network."/></label><button className="button button-primary"disabled={busy}>{busy?"Submitting…":"Submit private vouch"}</button></form></section><section className="referral-history"><header><div><p className="eyebrow">Your invitations</p><h2>Referral journey</h2></div><span>{referrals.filter(item=>item.status==="activated").length} activated</span></header>{referrals.length?<div>{referrals.map(item=><article key={item.referral_id}><div><strong>{item.invitee_email}</strong><small>{item.campaign_name} · {new Intl.DateTimeFormat("en-KE",{dateStyle:"medium"}).format(new Date(item.created_at))}</small></div><span className={`member-status status-${item.status}`}>{item.status.replace("_"," ")}</span><p>{item.relationship}</p>{item.review_note&&["rejected","revoked"].includes(item.status)?<small>{item.review_note}</small>:null}</article>)}</div>:<div className="admin-empty"><strong>No referrals submitted</strong><p>Use your invitation thoughtfully when the right woman comes to mind.</p></div>}</section>{message?<p className="network-message"role="status">{message}</p>:null}</>}
