"use client";import{useMemo,useState}from"react";import{createClient}from"@/lib/supabase/client";
export function LessonProgressControl({lessonId,complete}:{lessonId:string;complete:boolean}){const supabase=useMemo(()=>createClient(),[]);const[done,setDone]=useState(complete);const[busy,setBusy]=useState(false);async function mark(){setBusy(true);const{error}=await supabase.rpc("save_lesson_progress",{p_lesson_id:lessonId,p_position:null,p_progress:100});setBusy(false);if(!error)setDone(true)}return <button className={done?"lesson-complete":""}disabled={busy||done}onClick={()=>void mark()}>{done?"Completed":"Mark complete"}</button>}
