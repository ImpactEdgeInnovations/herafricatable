"use client";
import Link from"next/link";import{useMemo,useState}from"react";import{createClient}from"@/lib/supabase/client";

export type CommunitySummary={community_id:string;slug:string;name:string;description:string;community_type:"official"|"private";status:string;membership_status:string|null;membership_role:string|null;member_count:number;pending_count:number};

export function CommunityDirectory({communities}:{communities:CommunitySummary[]}){const supabase=useMemo(()=>createClient(),[]);const[busy,setBusy]=useState("");const[message,setMessage]=useState("");async function join(item:CommunitySummary){setBusy(item.community_id);const{error}=item.membership_status==="invited"?await supabase.rpc("respond_to_community_invitation",{p_accept:true,p_community_id:item.community_id}):await supabase.rpc("request_community_access",{p_community_id:item.community_id});setBusy("");setMessage(error?error.message:item.community_type==="official"?"Welcome to the community.":"Your request has been sent to the hosts.");if(!error)window.location.reload()}return <><section className="community-grid">{communities.map(item=><article key={item.community_id}><div className="community-card-meta"><span>{item.community_type}</span><small>{item.member_count} member{Number(item.member_count)===1?"":"s"}</small></div><h2>{item.name}</h2><p>{item.description}</p><footer>{item.membership_status==="active"?<Link className="button button-primary"href={`/communities/${item.slug}`}>Enter community</Link>:item.membership_status==="requested"?<span>Membership request pending</span>:<button className="button button-outline"disabled={busy===item.community_id}onClick={()=>void join(item)}>{item.membership_status==="invited"?"Accept invitation":item.community_type==="private"?"Request access":"Join community"}</button>}</footer></article>)}</section>{message?<p className="network-message"role="status">{message}</p>:null}</>}
