"use client";import{useMemo,useState}from"react";import{createClient}from"@/lib/supabase/client";import{useActionDialog}from"@/components/ui/action-dialog";
export type PartnerPerk={perk_id:string;slug:string;title:string;description:string;terms:string;partner_id:string;partner_name:string;partner_description:string;partner_website:string|null;partner_logo:string|null;category:string;city:string|null;country:string;inventory_total:number|null;inventory_remaining:number|null;per_member_limit:number;starts_at:string;ends_at:string;status:string;own_redemption_id:string|null;own_redemption_code:string|null;own_redemption_status:string|null;own_expires_at:string|null};
export function PerksGallery({perks}:{perks:PartnerPerk[]}){const supabase=useMemo(()=>createClient(),[]);const[busy,setBusy]=useState("");const[message,setMessage]=useState("");const{ask,dialog}=useActionDialog();async function reserve(id:string){const result=await ask({title:"Reserve this benefit?",description:"A private redemption code will be created for you. It will expire if it is not redeemed in time.",confirmLabel:"Reserve benefit"});if(!result)return;setBusy(id);const{error}=await supabase.rpc("reserve_partner_perk",{p_perk_id:id});setBusy("");setMessage(error?error.message:"Your private redemption code is ready.");if(!error)location.reload()}return <section className="perks-grid">{dialog}{perks.length?perks.map(perk=><article key={perk.perk_id}><header><div><span>{perk.category}</span><strong>{perk.partner_name}</strong></div><small>{[perk.city,perk.country].filter(Boolean).join(" · ")}</small></header><h2>{perk.title}</h2><p>{perk.description}</p><details><summary>Terms and eligibility</summary><p>{perk.terms}</p></details><footer><div><small>Available until</small><strong>{new Intl.DateTimeFormat("en-KE",{dateStyle:"medium"}).format(new Date(perk.ends_at))}</strong>{perk.inventory_remaining!==null?<span>{perk.inventory_remaining} remaining</span>:null}</div>{perk.own_redemption_status?<div className="perk-code"><small>{perk.own_redemption_status}</small><strong>{perk.own_redemption_code}</strong>{perk.own_expires_at?<span>Use by {new Intl.DateTimeFormat("en-KE",{dateStyle:"medium"}).format(new Date(perk.own_expires_at))}</span>:null}</div>:<button className="button button-primary"disabled={busy===perk.perk_id||perk.inventory_remaining===0}onClick={()=>void reserve(perk.perk_id)}>{busy===perk.perk_id?"Reserving…":perk.inventory_remaining===0?"Fully reserved":"Reserve benefit"}</button>}</footer></article>):<div className="admin-empty"><strong>No partner benefits are open</strong><p>Curated offers will appear here only after their terms, inventory and redemption owner are approved.</p></div>}{message?<p className="network-message"role="status">{message}</p>:null}</section>}
