"use client";import{useMemo,useState}from"react";import{createClient}from"@/lib/supabase/client";
export type CommunityReport={report_id:string;community_id:string;community_name:string;reporter_email:string;category:string;details:string;evidence_snapshot:Record<string,unknown>;status:string;created_at:string};
export function CommunityModeration({reports,migrationReady}:{reports:CommunityReport[];migrationReady:boolean}){const supabase=useMemo(()=>createClient(),[]);const[busy,setBusy]=useState("");const[message,setMessage]=useState("");async function review(id:string,action:string){const outcome=action==="start_review"?"":window.prompt(action==="hide"?"Why should this post be hidden?":"Why is this report being dismissed?")??"";if(action!=="start_review"&&outcome.trim().length<5)return;setBusy(id);const{error}=await supabase.rpc("review_community_report",{p_action:action,p_outcome:outcome,p_report_id:id});setBusy("");setMessage(error?error.message:"Community moderation decision recorded.");if(!error)window.location.reload()}if(!migrationReady)return null;return <section className="admin-section moderation-queue"id="community-moderation"><div className="admin-section-heading"><div><p className="eyebrow">Report-scoped access</p><h2>Community safety</h2><p>Moderators receive only captured evidence from reported posts, never general access to private community feeds.</p></div><span>{reports.filter(item=>["open","reviewing"].includes(item.status)).length} active</span></div>{reports.length?<div>{reports.map(report=><article key={report.report_id}><div><span className="member-status">{report.status}</span><small>{report.community_name} · {report.category}</small></div><div><strong>{report.reporter_email}</strong><p>{report.details}</p><blockquote>{String(report.evidence_snapshot.body??"")}</blockquote></div>{["open","reviewing"].includes(report.status)?<div className="member-actions">{report.status==="open"?<button disabled={busy===report.report_id}onClick={()=>void review(report.report_id,"start_review")}>Start review</button>:null}<button className="danger-action"disabled={busy===report.report_id}onClick={()=>void review(report.report_id,"hide")}>Hide post</button><button disabled={busy===report.report_id}onClick={()=>void review(report.report_id,"dismiss")}>Dismiss</button></div>:null}</article>)}</div>:<div className="admin-empty"><strong>No community reports</strong><p>Reported post snapshots will appear here for bounded review.</p></div>}{message?<p className="manager-message content-manager-message">{message}</p>:null}</section>}
