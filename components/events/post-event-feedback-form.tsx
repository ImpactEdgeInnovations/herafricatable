"use client";
import{FormEvent,useMemo,useState}from"react";import{createClient}from"@/lib/supabase/client";
export type ExistingEventFeedback={connection_rating:number;highlight:string|null;improvement:string|null;overall_rating:number;relevance_rating:number;testimonial_consent:string;testimonial_quote:string|null;testimonial_status:string;would_recommend:boolean};
function Rating({label,name,value}:{label:string;name:string;value:number}){return <fieldset className="event-rating"><legend>{label}</legend>{[1,2,3,4,5].map(rating=><label key={rating}><input type="radio" name={name} value={rating} defaultChecked={value===rating} required/><span>{rating}</span></label>)}</fieldset>}
export function PostEventFeedbackForm({eventId,eventTitle,existing}:{eventId:string;eventTitle:string;existing:ExistingEventFeedback|null}){const supabase=useMemo(()=>createClient(),[]);const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");const[consent,setConsent]=useState(existing?.testimonial_consent??"none");async function save(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);setBusy(true);const{error}=await supabase.rpc("save_event_feedback",{p_connections:Number(form.get("connections")),p_event_id:eventId,p_highlight:form.get("highlight"),p_improvement:form.get("improvement"),p_overall:Number(form.get("overall")),p_relevance:Number(form.get("relevance")),p_testimonial_consent:form.get("testimonial_consent"),p_testimonial_quote:form.get("testimonial_quote"),p_would_recommend:form.get("recommend")==="yes"});setBusy(false);setMessage(error?error.message:"Thank you. Your private event feedback has been saved.");if(!error)window.scrollTo({top:0,behavior:"smooth"})}async function withdraw(){setBusy(true);const{error}=await supabase.rpc("withdraw_event_testimonial",{p_event_id:eventId});setBusy(false);setMessage(error?error.message:"Testimonial permission withdrawn. Your private event feedback remains saved.");if(!error)setConsent("none")}return <section className="event-feedback-shell"><header><p className="eyebrow">After the table</p><h1>Help us shape what comes next.</h1><p>Your ratings and written feedback are private to the event team. Nothing becomes a public testimonial without your explicit permission and a separate review.</p>{message?<div className="feedback-message" role="status">{message}</div>:null}</header><form onSubmit={event=>void save(event)}><div className="feedback-event-name"><span>Reflecting on</span><strong>{eventTitle}</strong></div><Rating label="Overall experience" name="overall" value={existing?.overall_rating??0}/><Rating label="Professional relevance" name="relevance" value={existing?.relevance_rating??0}/><Rating label="Quality of connections" name="connections" value={existing?.connection_rating??0}/><fieldset className="recommend-field"><legend>Would you recommend a Her Africa Table event?</legend><label><input type="radio" name="recommend" value="yes" defaultChecked={existing?.would_recommend!==false}required/>Yes</label><label><input type="radio" name="recommend" value="no" defaultChecked={existing?.would_recommend===false}/>Not yet</label></fieldset><label>What stayed with you?<textarea name="highlight" maxLength={2000} defaultValue={existing?.highlight??""} placeholder="A moment, conversation or insight…"/></label><label>What should we improve?<textarea name="improvement" maxLength={2000} defaultValue={existing?.improvement??""} placeholder="Specific feedback helps us act."/></label><section className="testimonial-consent"><div><span>Optional testimonial</span><strong>Your words, your choice.</strong><p>If you offer a quote, choose exactly how it may be attributed. You can withdraw permission later.</p></div><label>Permission<select name="testimonial_consent" value={consent} onChange={event=>setConsent(event.target.value)}><option value="none">Keep everything private</option><option value="anonymous">May publish anonymously</option><option value="named">May publish with my member name</option></select></label>{consent!=="none"?<label className="wide">Quote<textarea name="testimonial_quote" minLength={20} maxLength={1000} required defaultValue={existing?.testimonial_quote??""} placeholder="Write the exact words you permit Her Africa Table to reuse."/><small>Consent version 2026-07-22 · Publication still requires team approval.</small></label>:<input type="hidden" name="testimonial_quote" value=""/>}{existing&&existing.testimonial_consent!=="none"?<button type="button" className="text-button" disabled={busy}onClick={()=>void withdraw()}>Withdraw testimonial permission</button>:null}</section><button className="button button-primary" disabled={busy}>{busy?"Saving…":existing?"Update feedback":"Submit private feedback"}</button></form></section>}
