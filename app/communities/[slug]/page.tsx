import Link from"next/link";import{notFound,redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{CommunityFeed,type CommunityPost}from"@/components/member/community-feed";import type{CommunitySummary}from"@/components/member/community-directory";
export const dynamic="force-dynamic";
export default async function CommunityPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/sign-in");const{data:flag}=await supabase.from("feature_flags").select("enabled").eq("key","communities").maybeSingle();if(!flag?.enabled)redirect("/communities");const communitiesResult=await supabase.rpc("list_communities");const community=((communitiesResult.data as CommunitySummary[]|null)??[]).find(item=>item.slug===slug);if(!community)notFound();if(community.membership_status!=="active")redirect("/communities");const postsResult=await supabase.rpc("list_community_posts",{p_community_id:community.community_id,p_limit:30,p_offset:0});return <main className="community-page"><header className="member-home-header"><Link className="brand"href="/"><span className="brand-mark">H</span><span>Her Africa Table<small>{community.name}</small></span></Link><nav><Link href="/communities">All communities</Link><Link href="/home">Member home</Link></nav></header><section className="community-room-hero"><div><p className="eyebrow">{community.community_type} community</p><h1>{community.name}</h1><p>{community.description}</p></div><span>{community.member_count} members</span></section>{postsResult.error?<section className="admin-empty opportunity-error"><strong>Community feed unavailable</strong><p>{postsResult.error.message}</p></section>:<CommunityFeed communityId={community.community_id}currentUserId={user.id}initialPosts={(postsResult.data as CommunityPost[]|null)??[]}/>}</main>}
