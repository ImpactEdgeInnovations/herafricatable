import Link from"next/link";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{CommunityDirectory,type CommunitySummary}from"@/components/member/community-directory";
import {MemberHeader} from "@/components/member/member-header";
export const dynamic="force-dynamic";
export default async function CommunitiesPage(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/sign-in");const{data:profile}=await supabase.from("profiles").select("access_status").eq("id",user.id).maybeSingle();if(profile?.access_status!=="active")redirect("/home");const{data:flag}=await supabase.from("feature_flags").select("enabled").eq("key","communities").maybeSingle();if(!flag?.enabled)return <main className="community-page"><MemberHeader active="community" label="Community"/><section className="community-hold"><p className="eyebrow">Your community home</p><h1>The first table is being prepared.</h1><p>Private spaces open only after their host, safety boundaries and member experience have passed release review. Your membership and existing connections remain unchanged.</p><div className="journey-state-actions"><Link className="button button-primary"href="/events">View gatherings</Link><Link className="button button-outline"href="/home">Return home</Link></div></section></main>;const result=await supabase.rpc("list_communities");return <main className="community-page"><MemberHeader active="community" label="Community"/><section className="community-hero"><p className="eyebrow">Your community home</p><h1>Belong with<br/>shared purpose.</h1><p>Enter a trusted room to introduce yourself, exchange practical support and continue relationships beyond each gathering.</p></section>{result.error?<section className="admin-empty opportunity-error"><strong>Community is temporarily unavailable</strong><p>Please try again or contact support if the problem continues.</p><div className="journey-state-actions"><Link className="button button-primary"href="/communities">Try again</Link><Link className="button button-outline"href="/support">Contact support</Link></div></section>:<CommunityDirectory communities={(result.data as CommunitySummary[]|null)??[]}/>}</main>}
