import Link from "next/link";
import { notFound,redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
export const dynamic="force-dynamic";
export default async function ReceiptPage({params}:{params:Promise<{reference:string}>}){const {reference}=await params;const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/sign-in");const {data}=await supabase.from("orders").select("id,reference,status,processing_mode,currency,subtotal_minor,total_minor,fulfilled_at,created_at,events(title,starts_at,timezone,venues(name,city,country)),order_items(quantity,unit_price_minor,line_total_minor,ticket_types(name,description)),payment_attempts(provider,provider_reference,status,created_at)").eq("reference",reference).eq("user_id",user.id).maybeSingle();if(!data)notFound();const event=data.events as unknown as {starts_at:string;timezone:string;title:string;venues:{city:string;country:string;name:string}|null}|null;const items=data.order_items as unknown as {line_total_minor:number;quantity:number;ticket_types:{description:string|null;name:string}|null;unit_price_minor:number}[];return <main className="receipt-page"><header className="legal-header"><Link className="brand" href="/"><span className="brand-mark">H</span><span>Her Africa Table<small>Registration record</small></span></Link><Link href="/home">Member home</Link></header><article className="receipt-card"><header><div><p className="eyebrow">Order record</p><h1>{event?.title??"Her Africa Table"}</h1></div><span className="member-status">{data.status.replace("_"," ")}</span></header><dl><div><dt>Reference</dt><dd>{data.reference}</dd></div><div><dt>Created</dt><dd>{new Intl.DateTimeFormat("en-KE",{dateStyle:"long"}).format(new Date(data.created_at))}</dd></div>{event?<div><dt>Event date</dt><dd>{new Intl.DateTimeFormat("en-KE",{dateStyle:"full",timeZone:event.timezone}).format(new Date(event.starts_at))}</dd></div>:null}<div><dt>Processing</dt><dd>{data.processing_mode.replace("_"," ")}</dd></div></dl><div className="receipt-lines">{items.map((item,index)=><div key={index}><span>{item.ticket_types?.name} × {item.quantity}</span><strong>{data.currency} {(item.line_total_minor/100).toLocaleString("en-KE",{minimumFractionDigits:2})}</strong></div>)}<div className="receipt-total"><span>Total</span><strong>{data.currency} {(data.total_minor/100).toLocaleString("en-KE",{minimumFractionDigits:2})}</strong></div></div><p className="receipt-note">This record confirms platform order status. Payment is considered complete only when the status is fulfilled. For support, quote reference <strong>{data.reference}</strong>.</p></article></main>}
