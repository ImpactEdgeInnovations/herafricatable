import Link from"next/link";import{createClient}from"@/lib/supabase/server";
export const dynamic="force-dynamic";
type PastEvent={city:string|null;country:string|null;ends_at:string;event_id:string;format:string;highlights:string[];recap_summary:string|null;recap_title:string|null;slug:string;starts_at:string;summary:string|null;timezone:string;title:string;venue_name:string|null};
export default async function PastEventsPage(){const supabase=await createClient();const[{data:past},{data:{user}}]=await Promise.all([supabase.rpc("list_public_past_events",{p_limit:30,p_offset:0}),supabase.auth.getUser()]);const{data:mine}=user?await supabase.rpc("list_my_past_events"):{data:[]};const eligible=new Map(((mine as {feedback_id:string|null;slug:string}[]|null)??[]).map(item=>[item.slug,item.feedback_id]));const events=(past as PastEvent[]|null)??[];const testimonialResults=await Promise.all(events.map(event=>supabase.rpc("list_event_testimonials",{p_event_id:event.event_id})));const testimonials=new Map(events.map((event,index)=>[event.event_id,(testimonialResults[index].data as {attribution:string;quote:string}[]|null)??[]]));return <main className="past-events-page"><header className="legal-header"><Link className="brand"href="/"><span className="brand-mark">H</span><span>Her Africa Table<small>Past tables</small></span></Link><Link href="/events">Upcoming events</Link></header><section className="past-events-hero"><p className="eyebrow">The table continues</p><h1>Past gatherings.<br/>Lasting connections.</h1><p>Return to the ideas, people and moments that moved through each room.</p></section><section className="past-event-list">{events.length?events.map(event=><article id={event.event_id}key={event.event_id}><aside><span>{new Intl.DateTimeFormat("en-KE",{month:"short"}).format(new Date(event.starts_at))}</span><strong>{new Intl.DateTimeFormat("en-KE",{day:"2-digit"}).format(new Date(event.starts_at))}</strong><small>{new Intl.DateTimeFormat("en-KE",{year:"numeric"}).format(new Date(event.starts_at))}</small></aside><div><p className="eyebrow">{event.format.replace("_"," ")} · {[event.venue_name,event.city].filter(Boolean).join(", ")||"Online"}</p><h2>{event.title}</h2><p>{event.recap_summary||event.summary||"A Her Africa Table gathering."}</p>{event.highlights?.length?<ul>{event.highlights.map(highlight=><li key={highlight}>{highlight}</li>)}</ul>:null}{testimonials.get(event.event_id)?.length?<div className="past-testimonials">{testimonials.get(event.event_id)?.map(item=><blockquote key={`${item.attribution}-${item.quote}`}>“{item.quote}”<cite>— {item.attribution}</cite></blockquote>)}</div>:null}{eligible.has(event.slug)?<Link className="button button-outline"href={`/events/${event.slug}/feedback`}>{eligible.get(event.slug)?"Update your feedback":"Share private feedback"}</Link>:null}</div></article>):<div className="events-empty"><strong>Our first chapter is still ahead.</strong><p>Completed gatherings and approved recaps will live here.</p><Link className="button button-primary"href="/events">Explore upcoming tables</Link></div>}</section></main>}
