import Link from"next/link";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{CirclesHub,type CircleCycle,type MyCircle,type CircleMember,type CirclePrompt,type CircleResponse}from"@/components/member/circles-hub";
export const dynamic="force-dynamic";
export default async function CirclesPage({searchParams}:{searchParams:Promise<{circle?:string}>}){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/sign-in");const{data:profile}=await supabase.from("profiles").select("access_status").eq("id",user.id).maybeSingle();if(profile?.access_status!=="active")redirect("/home");const{data:flag}=await supabase.from("feature_flags").select("enabled").eq("key","circles").maybeSingle();if(!flag?.enabled)return <main className="circles-page"><header className="member-home-header"><Link className="brand"href="/"><span className="brand-mark">H</span><span>Her Africa Table<small>Circles</small></span></Link><nav><Link href="/home">Member home</Link></nav></header><section className="community-hold"><p className="eyebrow">Small rooms, deliberate chemistry</p><h1>Circles are being prepared.</h1><p>Matching will open only after the cycle, facilitation and privacy boundaries pass review.</p><Link className="button button-outline"href="/home">Return home</Link></section></main>;const[cycleResult,circleResult]=await Promise.all([supabase.rpc("list_circle_cycles"),supabase.rpc("list_my_circles")]);const circles=(circleResult.data as MyCircle[]|null)??[];const requested=(await searchParams).circle;const selected=circles.some(c=>c.circle_id===requested)?requested??null:circles[0]?.circle_id??null;const[memberResult,promptResult]=selected?await Promise.all([supabase.rpc("list_circle_members",{p_circle_id:selected}),supabase.rpc("list_circle_prompts",{p_circle_id:selected})]):[{data:[]},{data:[]}];const prompts=(promptResult.data as CirclePrompt[]|null)??[];const responseResults=await Promise.all(prompts.map(prompt=>supabase.rpc("list_circle_responses",{p_prompt_id:prompt.prompt_id})));const responses=Object.fromEntries(prompts.map((prompt,index)=>[prompt.prompt_id,(responseResults[index].data as CircleResponse[]|null)??[]]));return <main className="circles-page"><header className="member-home-header"><Link className="brand"href="/"><span className="brand-mark">H</span><span>Her Africa Table<small>Circles</small></span></Link><nav><Link href="/home">Home</Link><Link href="/network">Network</Link><Link href="/messages">Messages</Link></nav></header><section className="circles-hero"><p className="eyebrow">Deterministic, human-reviewed matching</p><h1>A smaller table<br/>within the table.</h1><p>Time-bound cohorts shaped by shared context, complementary ambition and an explicit human release decision.</p></section>{cycleResult.error||circleResult.error?<section className="network-error"><strong>Circles are not ready.</strong><p>{cycleResult.error?.message??circleResult.error?.message}</p></section>:<CirclesHub cycles={(cycleResult.data as CircleCycle[]|null)??[]}circles={circles}selected={selected}members={(memberResult.data as CircleMember[]|null)??[]}prompts={prompts}responses={responses}/>}</main>}
