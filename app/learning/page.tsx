import Link from"next/link";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{LearningCatalog,type CourseSummary}from"@/components/member/learning-catalog";
export const dynamic="force-dynamic";
export default async function LearningPage(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/sign-in");const{data:profile}=await supabase.from("profiles").select("access_status").eq("id",user.id).maybeSingle();if(profile?.access_status!=="active")redirect("/home");const{data:flag}=await supabase.from("feature_flags").select("enabled").eq("key","learning").maybeSingle();if(!flag?.enabled)return <main className="learning-page"><header className="member-home-header"><Link className="brand"href="/"><span className="brand-mark">H</span><span>Her Africa Table<small>Learning</small></span></Link><nav><Link href="/home">Member home</Link></nav></header><section className="community-hold"><p className="eyebrow">Knowledge with context</p><h1>The learning studio is being prepared.</h1><p>Courses will open only after their content, access rules and payment paths have passed release review.</p><Link className="button button-outline"href="/home">Return to member home</Link></section></main>;const result=await supabase.rpc("list_courses");return <main className="learning-page"><header className="member-home-header"><Link className="brand"href="/"><span className="brand-mark">H</span><span>Her Africa Table<small>Learning studio</small></span></Link><nav><Link href="/home">Member home</Link><Link href="/communities">Communities</Link><Link href="/network">Network</Link></nav></header><section className="learning-hero"><p className="eyebrow">Practice, perspective, progress</p><h1>Learn what moves<br/>your work forward.</h1><p>Focused learning experiences created for the realities, ambitions and opportunities of African women.</p></section>{result.error?<section className="admin-empty opportunity-error"><strong>Learning database update required</strong><p>Apply the latest learning migration, then reload.</p></section>:<LearningCatalog courses={(result.data as CourseSummary[]|null)??[]}/>}</main>}
