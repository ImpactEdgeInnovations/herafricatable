import Link from"next/link";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{PerksGallery,type PartnerPerk}from"@/components/member/perks-gallery";export const dynamic="force-dynamic";
export default async function PerksPage(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/sign-in");const{data:profile}=await supabase.from("profiles").select("access_status").eq("id",user.id).maybeSingle();if(profile?.access_status!=="active")redirect("/home");const{data:flag}=await supabase.from("feature_flags").select("enabled").eq("key","partner_perks").maybeSingle();if(!flag?.enabled)return <main className="perks-page"><header className="member-home-header"><Link className="brand"href="/"><span className="brand-mark">H</span><span>Her Africa Table<small>Partner benefits</small></span></Link><nav><Link href="/home">Member home</Link></nav></header><section className="community-hold"><p className="eyebrow">Useful, carefully negotiated</p><h1>Partner benefits are being curated.</h1><p>Offers will open only after their terms, inventory and redemption process pass review.</p><Link className="button button-outline"href="/home">Return home</Link></section></main>;const{data,error}=await supabase.rpc("list_partner_perks");return <main className="perks-page"><header className="member-home-header"><Link className="brand"href="/"><span className="brand-mark">H</span><span>Her Africa Table<small>Partner benefits</small></span></Link><nav><Link href="/home">Home</Link><Link href="/support">Support</Link></nav></header><section className="perks-hero"><p className="eyebrow">Value beyond the gathering</p><h1>Benefits selected<br/>for women building.</h1><p>Thoughtfully negotiated access from partners aligned with the work, wellbeing and mobility of our members.</p></section>{error?<section className="network-error"><strong>Partner benefits are not ready.</strong><p>{error.message}</p></section>:<PerksGallery perks={(data as PartnerPerk[]|null)??[]}/>}</main>}
