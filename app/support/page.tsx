import Link from "next/link";
import { redirect } from "next/navigation";
import { SupportCenter, type SupportMessage, type SupportTicket } from "@/components/member/support-center";
import { createClient } from "@/lib/supabase/server";

export const dynamic="force-dynamic";
export default async function SupportPage({searchParams}:{searchParams:Promise<{ticket?:string}>}) {const {ticket}=await searchParams;const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/sign-in");const result=await supabase.from("support_tickets").select("id,reference,category,subject,description,status,priority,assigned_to,created_at,updated_at").order("updated_at",{ascending:false});const tickets=(result.data as SupportTicket[]|null)??[];const selected=ticket&&tickets.some(item=>item.id===ticket)?ticket:tickets[0]?.id??null;let messages:SupportMessage[]=[];if(selected){const {data}=await supabase.from("support_messages").select("id,ticket_id,author_id,body,is_staff,created_at").eq("ticket_id",selected).order("created_at",{ascending:true});messages=(data as SupportMessage[]|null)??[]}return <main className="support-page"><header className="member-home-header"><Link className="brand" href="/"><span className="brand-mark">H</span><span>Her Africa Table<small>Member care</small></span></Link><nav><Link href="/home">Member home</Link><Link href="/events">Events</Link></nav></header>{result.error?<section className="admin-empty network-error"><strong>Support database update required</strong><p>Apply <code>20260723170000_support_operations.sql</code>.</p></section>:<SupportCenter currentUserId={user.id} tickets={tickets} messages={messages} selectedTicketId={selected}/>}</main>}
